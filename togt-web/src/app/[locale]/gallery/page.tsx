import type { Metadata } from "next";
import Link from "next/link";
import { fetchPublic, pageMetadata, type SeoLocale } from "@/lib/seo";
type GalleryItem = { id: string; title: string; description: string; image: string; location?: string | null };
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; return pageMetadata(locale as SeoLocale, "gallery", "Travel Gallery | TOGT Tour & Travel", "See TOGT Tour & Travel experiences, milestones, destinations, and travel moments from Ethiopia and beyond."); }
export default async function GalleryPage({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; const items = await fetchPublic<GalleryItem[]>("/content/gallery") ?? []; return <main className="mx-auto max-w-7xl px-4 py-12"><h1 className="text-4xl font-black text-togt-navy">TOGT travel gallery</h1><p className="mt-3 text-slate-600">Travel experiences and milestones from TOGT Tour & Travel.</p><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{items.map((item) => <Link key={item.id} href={`/${locale}/gallery/${item.id}`} className="overflow-hidden rounded-2xl border bg-white shadow-sm transition hover:shadow-xl"><img src={item.image} alt={`${item.title}${item.location ? ` in ${item.location}` : ""}`} className="h-56 w-full object-cover" /><div className="p-4"><h2 className="font-black text-togt-navy">{item.title}</h2><p className="mt-1 line-clamp-2 text-sm text-slate-600">{item.description}</p></div></Link>)}</div></main>; }
