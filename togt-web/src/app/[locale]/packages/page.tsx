import type { Metadata } from "next";
import Link from "next/link";
import { JsonLd } from "@/components/seo/json-ld";
import { fetchPublic, pageMetadata, type SeoLocale } from "@/lib/seo";

type PackageItem = { id: string; title: string; description: string; price?: number | null; currency?: string | null; duration?: string | null; image?: string | null };
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; return pageMetadata(locale as SeoLocale, "packages", "Travel Packages from Ethiopia | TOGT Tour & Travel", "Explore Umrah, domestic, foreigner, and international travel packages from Addis Ababa with TOGT Tour & Travel."); }
export default async function PackagesPage({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; const items = await fetchPublic<PackageItem[]>("/packages") ?? []; return <main className="mx-auto max-w-7xl px-4 py-12"><JsonLd data={{ "@context": "https://schema.org", "@type": "CollectionPage", name: "TOGT Travel Packages", url: `https://travel.togttrading.com/${locale}/packages` }} /><h1 className="text-4xl font-black text-togt-navy">Travel packages from Ethiopia</h1><p className="mt-3 max-w-2xl text-slate-600">Umrah packages, Ethiopian tours, foreigner tours, and international travel designed with support from Addis Ababa.</p><div className="mt-10 grid gap-5 md:grid-cols-3">{items.map((item) => <Link key={item.id} href={`/${locale}/packages/${item.id}`} className="overflow-hidden rounded-2xl border bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl">{item.image && <img src={item.image} alt={`${item.title} travel package`} className="h-48 w-full object-cover" />}<div className="p-5"><h2 className="text-xl font-black text-togt-navy">{item.title}</h2><p className="mt-2 line-clamp-3 text-sm text-slate-600">{item.description}</p><p className="mt-4 font-bold text-togt-orange">{item.price != null ? `${item.price.toLocaleString()} ${item.currency ?? "ETB"}` : "Custom pricing"}{item.duration ? ` · ${item.duration}` : ""}</p></div></Link>)}</div></main>; }
